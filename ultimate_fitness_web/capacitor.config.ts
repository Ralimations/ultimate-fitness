import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ultimatefitness.app',
  appName: 'ultimate_fitness_web',
  webDir: 'dist'
};

export default config;
